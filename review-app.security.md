4. Security & Permissions Doc

Outline:

Describe roles and permissions.

Sketch Firestore rules (you can paste from earlier answer).

Describe auth checks in UI:

Hide/disable owner actions if not owner.

Hide admin actions if not admin.

Describe server-side enforcement via Security Rules + Cloud Functions for sensitive operations (e.g. claim approval).